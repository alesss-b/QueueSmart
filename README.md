# QueueStar

About QueueStar...

## Installation
1. Install WSL
2. Install Docker Engine
3. Clone repository
4. Run `$ docker build -t django-docker .` in project root directory
5. `$ docker compose up`